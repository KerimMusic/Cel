
// Espera a que el DOM esté completamente cargado
document.addEventListener("DOMContentLoaded", () => {
    
    // Añade la clase 'loaded' al body para activar la transición de opacidad en CSS
    document.body.classList.add("loaded");
    
    // Mensaje en la consola para verificar que el script funciona
    console.log("Omega Beats - Página de información cargada correctamente.");
    
});
